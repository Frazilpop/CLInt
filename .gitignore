# Per-machine user data
data/

# Pre-v0.2.10 layout kept the data files at the root; still ignored so
# old installs stay clean in git until CLInt's first launch migrates them.
settings.json
tdp-settings.json
menu-key.txt
recent.json
watch-history.json
update-available.txt
error.log

# Claude Code dev-machine files (contain personal paths)
CLAUDE.md
.claude/
